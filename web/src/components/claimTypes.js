import React from 'react';
import { Layout, Menu, Breadcrumb, Icon } from "antd";
//import { FirebaseDB } from "../constants/firebase";

const { Header, Content, Footer, Sider } = Layout;
const { SubMenu } = Menu;
const test = () => {
    return(
        <div>
            rendering claim types
        </div>
    );
}

export default test;